sudo: False
language: python
python:
  - 2.6
  - 2.7
  - 3.3
  - 3.4
  - pypy
install:
  - pip install pybuilder
  - pyb install_dependencies
script: "pyb -X && PYTHONPATH=src/main/python src/main/scripts/netconns"
after_success:
  - pip install coveralls
  - coveralls --verbose
